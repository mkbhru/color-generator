Search Color with hexadecimal codes, provides same color in different shades.Directly copy the hexadecimal codes of
different shades in clipboard to use in other projects.
Used React-toastify to generate messages for copy or errors.
